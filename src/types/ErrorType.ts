export enum ErrorType {
  LOGIN = 'Login error',
  AUTH_WARN = 'Authorization error',
  PAGE_NOT_FOUND = 'Page not found',
  GET_PRODUCTS = 'Unable to get the products',
  GET_PRODUCT_DETAILS = 'Unable to get the product\'s details',
}
